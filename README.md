# Capacity Connect

Capacity Connect is a competency-driven learning and trainer matching platform for meteorological organizations.

## Development

```bash
npm install
npm run dev
```

The development command starts the Vite client and Express API together.

## Production

```bash
npm run build
npm start
```

Set `DATABASE_URL` when using the database package and keep local secrets in `.env`